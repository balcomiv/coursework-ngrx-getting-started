// import { ProductState } from '../products/state/product.reducer'; //  Fix Me - This breaks the lazy loaded boundary

export interface State {
    //  Fix Me - This breaks the lazy loaded boundary
    // products: ProductState;
    //  TODO: This will be strongly typed from the homework
    user: any;
}
